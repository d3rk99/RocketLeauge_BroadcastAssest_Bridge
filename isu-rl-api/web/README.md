# ISU RL API - Web Pages

## overlay/
Public OBS browser source page. Reads `/state/current` and listens on `/ws`.

## admin/
Simple operator console for team setup, mode switching, reset, and manual score control.

Both are plain HTML/CSS/JS and can be hosted statically.
